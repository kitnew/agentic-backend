[project]
name = "voice-agent"
version = "0.1.0"
description = "LiveKit voice agent runtime"
authors = [
    { name = "Nikita Chernysh", email = "h3s0y4mchik@gmail.com" }
]
requires-python = ">=3.14.6"
dependencies = [
    "contracts",
    "agentic-observability",
    "httpx>=0.28.1",
    "livekit-agents>=1.7.1",
    "livekit-plugins-elevenlabs==1.7.1",
    "livekit-plugins-openai==1.7.1",
    "pydantic-settings>=2.14.2",
    "pyjwt>=2.13.0",
]

[tool.uv.sources]
contracts = { workspace = true }
agentic-observability = { workspace = true }

[build-system]
requires = ["uv_build>=0.11.28,<0.12.0"]
build-backend = "uv_build"

[project.scripts]
up = "voice_agent.main:main"
