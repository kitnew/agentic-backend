[project]
name = "job-worker"
version = "0.1.0"
description = "Durable asynchronous jobs execution worker"
authors = [
    { name = "Nikita Chernysh", email = "h3s0y4mchik@gmail.com" }
]
requires-python = ">=3.14.6"
dependencies = [
    "contracts",
    "agentic-observability",
    "google-auth[requests]>=2.40.0",
    "httpx>=0.28.1",
    "jsonata-python==0.7.0",
    "jsonschema>=4.25.1",
    "minio>=7.2.18",
    "pyjwt>=2.13.0",
    "redis>=8.0.1",
    "opentelemetry-instrumentation-httpx>=0.60b1",
    "opentelemetry-instrumentation-redis>=0.60b1",
]

[tool.uv.sources]
contracts = { workspace = true }
agentic-observability = { workspace = true }


[build-system]
requires = ["uv_build>=0.11.28,<0.12.0"]
build-backend = "uv_build"

[project.scripts]
up = "job_worker.main:main"
